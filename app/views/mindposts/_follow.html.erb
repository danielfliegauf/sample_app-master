<%= form_for(current_user.mindposts.build, remote: true) do |f| %>
	<%= f.hidden_field :micropost_id, value: @micropost.id %>
	<%= f.submit "starempty", class: "lsf btn btn-primary btn-small", style: "color: #FFF; font-size: 25px;", title: t('remember') %>
<% end %>