<%= form_for(current_user.mindposts.find_by_micropost_id(@micropost), html: { method: :delete }, remote: true) do |f| %>
	<%= f.submit "star", class: "lsf btn btn-primary btn-small", style: "color: #FFF; font-size: 25px;", title: t('noremember')%>
<% end %>