<a href="<%= user_path(current_user) %>">
	<%= gravatar_for current_user, size: "110" %>
</a>
<h3>
	<%= current_user.name %>
</h3>
<span>
	<%= link_to t('link.profile'), current_user %>
</span>
<span>
	<%= pluralize(current_user.microposts.count, "micropost") %>
</span>