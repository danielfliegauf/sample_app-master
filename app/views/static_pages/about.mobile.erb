<% provide(:title, 'Options') %>

<% if signed_in? %>
<div>
	<div data-role="controlgroup" data-type="horizontal"></div>
    <div class="center" style="padding-left: 16%;">
      <section>
        <%= render 'shared/user_info' %>
      </section>
    </div>

</div>
<div style="clear: both;">
	<ul class="microposts" data-role="listview">
		<li><%= link_to t('link.manage_micropost'), current_user %></li>
		<li><%= link_to t('link.take_tour'), help_path %></li>
		<li><%= link_to t('link.write_us'), contact_path %></li>
		<li><%= link_to t('link.read_terms'), terms_path %></li>
		<li>
			<% if I18n.locale == I18n.default_locale %>
				<%= link_to "Ändere die Sprache (Englisch)", '/en'%>
			<% else %>
				<%= link_to "Change language (German)", '/de'%>
			<%end%>
		</li>	
		<li><%= link_to t('link.signout'), signout_path %></li>
	</ul>
</div>
<% else %>

<div class="center hero-unit">
<h1>Connectify</h1>
<p>
  ... a great place to <b>find people</b> doing the things you love most, too!
  
</p>It's just one click away... <br/><br />
<%= link_to "Connect me!", "/auth/facebook", :rel =>"external", :'data-role' => 'button', :'data-corners' => 'false', :'data-shadow' => 'false' %>
</div>

<% end %>